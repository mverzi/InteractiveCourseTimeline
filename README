After cloning, run the following in your command line inside the project folder:
1. npm install
Then:
1. Create a new file with the name: .env
2. Inside .env, write DB_STRING = <database string here>
